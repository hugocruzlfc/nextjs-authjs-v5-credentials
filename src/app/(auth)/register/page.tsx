import { RegisterForm } from "@/components/RegisterForm";
import { NextPage } from "next";

const Page: NextPage = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Page;
